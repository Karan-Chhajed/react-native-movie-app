import {Text, View} from 'react-native';
import React from 'react';

const Saved = () => {
  return (
    <View className="flex-1 items-center justify-center bg-white">
      <Text className="text-4xl text-blue-500">Saved</Text>
    </View>
  );
}

export default Saved;